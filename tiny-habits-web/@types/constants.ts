export const EMAIL_REGEX = /[\w-]+@([\w-]+\.)+[\w-]+/;
export const PASSWORD_REGEX = /^[\w!@#$_-]*$/;
export const NAVBAR_HEIGHT = 64;

export enum GOLDEN_ACTION_STEPS {
  ASPIRATION_INPUT,
  ACTIONS_INPUT,
  SELECT_EFFECTIVE,
  SELECT_REALISTIC,
  RESULT_AND_SAVE,
}
